<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<svg
   xmlns="http://www.w3.org/2000/svg"
   height="86"
   viewBox="0 0 247 86"
   width="247"
   version="1.1"
   id="svg74">
  <g
     transform="translate(0 8)"
     id="g72">
{% for bar in bars %}
    <rect
        {% for attr, value in bar.items %}
        {{ attr }}="{{ value }}"
        {% endfor %}
        />
{% endfor %}
  </g>
</svg>
