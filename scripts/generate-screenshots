#!/bin/sh

set -eu

WEBLATE=~/weblate/weblate/docs/screenshots/
SCREENSHOTS=weblate_web/static/img/

# Keep the full translation quality screenshot, removing only its 108px footer.
convert "$WEBLATE/translation-quality.webp" -gravity South -chop 0x108 +repage "$SCREENSHOTS/BigScreenshot.png"
